#ifndef SECRETS_H
#define SECRETS_H

/*
 * 私有凭据模板：复制本文件为同目录 secrets.h 并填入真实值。
 * secrets.h 已被 .gitignore 排除；任何情况下不要把真实值写回本模板。
 * 凭据轮换后建议同时在巴法云后台更新设备私钥。
 */
#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
#define BEMFA_UID "your-bemfa-uid"

#endif /* SECRETS_H */
